services:
  - type: web
    name: meeting-translator-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: APP_PROFILE
        value: demo
